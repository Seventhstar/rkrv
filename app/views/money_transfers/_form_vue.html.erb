<script>
  Vue.component('v-select', VueSelect.VueSelect)
  Vue.component('datepicker', vuejsDatepicker )

  let app = new Vue({
    el: '.vue_app',
    data: <%= fill_vue_data(@money_transfer, {
      model: 'money_transfer',
      noteValid: '',
      formValid: false,
      filteredProjects: [],
      texts: 'comment date',
      list_values: 'safe_from safe_to money_transfer_type user',
      lists: "safe_froms safe_tos money_transfer_types users"
    })
    %>,
      // user:   v_value(@money_transfer, "user", nil, current_user)
    
    updated(){
      this.onInput();
    },

    mounted(){
      this.$root.$on('onInput', this.onInput);
    },

    created(){
    },

    watch: {
    },

    methods: {
      onInput(e){

      }
    }
    
  });

</script>
