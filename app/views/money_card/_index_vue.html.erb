<script>
  Vue.component('v-select', VueSelect.VueSelect)

  let app = new Vue({
    el: '.expenses', 
    mixins: [m_created, m_index],
    data: <%= fill_vue_data(nil, { 
      sortKey: 'sortdate',
      controller: 'money_card',
      reverse: true,
      currentIndex: -1,
      currentMonth: -1,
      confirmModal: false,
      groupHeaders: [],
      filter: [],
      grouped: [],
      filteredData: [],
      list_values: '',
      lists: "mainList:raw@json_data",
      columns: @columns
      
    }) %>,
})
</script>