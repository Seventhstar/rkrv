<script>
  Vue.component('v-select', VueSelect.VueSelect)
  Vue.component('datepicker', vuejsDatepicker )

  let app = new Vue({
    el: '.vue_app',
    data: <%= fill_vue_data(@staff, {
      model: 'staff',
      noteValid: '',
      formValid: false,
      filteredProjects: [],
      texts: 'comment in_date out_date birthday medbook',
      list_values: 'department',
      lists: "departments"
    })
    %>,
    
    updated(){
      this.onInput();
    },

    mounted(){
      this.$root.$on('onInput', this.onInput);
    },

    created(){
    },

    watch: {
    },

    methods: {
      onInput(e){

      }
    }
    
  });

</script>
