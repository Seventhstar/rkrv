<script>
  Vue.component('v-select', VueSelect.VueSelect)
  Vue.component('datepicker', vuejsDatepicker )

  let app = new Vue({
    el: '.vue_app',
    data: <%= fill_vue_data(@expense, {
      model: 'expense',
      noteValid: '',
      formValid: false,
      filteredProjects: [],
      texts: 'comment date',
      list_values: 'safe expense_type department user',
      lists: "safes expense_types departments users"
    })
    %>,
    
    updated(){
      this.onInput();
    },

    mounted(){
      this.$root.$on('onInput', this.onInput);
    },

    created(){
    },

    watch: {
    },

    methods: {
      onInput(e) {

      }
    }
    
  });

</script>
